<!--
  README 骨架。使用说明：
  1. 所有 <尖括号> 内容都要替换掉，Scanner.ps1 -Mode readme 会把它们当占位符报出来
  2. 前 15 行是"首屏"，必须出现：项目名 + 一句话 + 截图 + 徽章
  3. 没有界面的项目删掉截图那行，不要留指向不存在文件的链接
  4. 完整的设计规则见 references/readme-design.md
-->

# <项目名>

<!-- 徽章按重要性排，2-4 个即可。许可证放第一位。没有 CI 就先只留许可证徽章 -->
![License](https://img.shields.io/badge/license-MIT-blue.svg)

<一句话说清这是什么、给谁用、关键差异点。公式：[给谁用] + [具体能做什么] + [不同在哪]。
 避免"基于 XXX 的 YYY 系统"这类同义反复，也不要写"提升效率 10 倍"这种空话。
 检验方法：把这句话单独发给不了解项目的同事，他能答出"这东西干嘛的"才算写对。>

<!-- 有界面就放截图——这是投入产出比最高的一项。图片放仓库内 docs/images/ 用相对路径引用，别用外链。
     动效类项目可以放 GIF，控制在 5MB 以内。不要放设计稿或概念图。 -->
![截图](docs/images/screenshot.png)

## 能做什么

<!-- 说能力，不说技术栈。读者不关心你用了什么框架，只关心能替他做什么 -->
- <能力一>
- <能力二>
- <能力三>

## 快速开始

<!-- 命令必须是从本仓库真实跑通的。每步都给完整命令，不要让读者自己猜 -->
```bash
git clone https://github.com/<owner>/<repo>.git
cd <repo>
<install command>
<run command>
```

打开 <地址或界面>，应该看到：<描述预期结果>

<!-- 上面那句"应该看到什么"很重要——它让读者能自己判断是否成功了 -->

## 使用

<!-- 一个从零到跑通的完整例子，比十行功能描述更有用 -->
```bash
<example>
```

## 配置

复制 `.env.example` 为 `.env` 后填写：

| 变量名 | 是否必填 | 说明 | 示例值 |
| --- | --- | --- | --- |
| `API_KEY` | 是 | <用途> | `your-api-key-here` |
| `LOG_LEVEL` | 否 | 日志级别，默认 `info` | `debug` |

> 示例值一律用占位符。真实密钥不要写进本文件，也不要提交 `.env`。

<!-- 配置项很多时，把不常用的折进 <details>，但安装步骤和许可证不要折 -->

## 不适用场景

<!-- 主动说清边界比罗列功能更能建立信任。读者踩坑后才发现限制，会留下差评 -->
- <什么情况下这个项目帮不上忙>
- <已知的性能或兼容性上限>

## 开发

```bash
# 安装开发依赖
<dev install>

# 运行测试
<test command>

# 代码检查
<lint command>
```

贡献流程见 [CONTRIBUTING.md](CONTRIBUTING.md)。

## 许可证

[MIT](LICENSE) © <年份> <版权人>

<!-- 必须与仓库根目录的 LICENSE 文件一致。只有文件没有这一段，读者不知道该不该用 -->
